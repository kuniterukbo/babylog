<%= render "shared/header" %>

<%= form_with model: @room, local: true do |f|%>
  <div>
    <%= f.label :赤ちゃんの名前 %>
    <%= f.text_field :babyname, placeholder: 'ルーム名を入力してください'%>
  </div>

  <div>
    <%= f.label :性別 %>
    <%= f.text_field :babyname, placeholder: 'ルーム名を入力してください'%>
  </div>

  <div class="input-birth-wrap">
    <%= f.label :生年月日 %>
     <%= raw sprintf(
                  f.date_select(
                    :birthday,
                    class:'select-birth',
                    id:"birth-date",
                    use_month_numbers: true,
                    prompt:'--',
                    start_year: 1930,
                    end_year: (Time.now.year - 5),
                    date_separator: '%s'),
                  "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" %>
  </div>
<% end %>